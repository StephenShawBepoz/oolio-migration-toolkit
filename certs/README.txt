Drop Epson printer .cer files in this folder before running module 3 step "Install Epson printer certs".

File naming convention: name each cert after its printer IP (e.g. 192.168.1.50.cer).

Generate certs via the Epson printer web interface, then copy them here.
