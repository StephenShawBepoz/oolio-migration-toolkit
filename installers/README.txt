Drop the Oolio POS native Windows installer in this folder (POS-*-installer.exe).

Module 4's "Install Oolio POS (native Windows app)" step runs the newest matching
installer silently (/S, per-machine) and creates the startup shortcut automatically.
If more than one installer is present, the most recently modified one is used.
